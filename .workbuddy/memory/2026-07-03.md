# 2026-07-03 工作日志

## 上午：前端部署到 Cloudflare Pages

- 项目完整推送到 GitHub（`git push -u origin master:main` 解决分支名不一致问题）
- GitHub 仓库改为公开，打通 Cloudflare 代码读取权限
- Cloudflare Pages 部署配置：Root=frontend, Build=npm run build, Output=dist, 监听路径=frontend/**
- 框架预设选 None（无 Vite 选项，选 None 即可正常构建）
- 线上地址：*.pages.dev，免费档，每月 500 次构建额度
- 后续迭代流程：本地 push → Cloudflare 自动构建 → 线上更新

## 下午：数据库设计 V2.1 + 后端基础设施 + 管理模块

### 数据库设计（基于真实 Excel 数据）
- 基于 7 个原始 Excel 数据源重构表结构，18 业务表 + 10 字典表
- 数据库设计规范 V1.1：字段类型约定同步更新
- Alembic 迁移修复：t_ingest_rows 缺失 + JSONB 默认值 '{}'::jsonb 语法
- 删库后 alembic upgrade head 一键生成全部 28 张表

### Seed 数据（583 行 SQL）
- 10 字典 + is_active=false 停用边界测试
- CX099 软删除合同（验证 WHERE deleted_at IS NULL）
- 完整合同链路：CX005 设计 3 阶段 + CX008 EPC 5 阶段，带月度明细和支付流水
- 四类进度款审核、周报形象+指标、省市月报、资金分解
- AI 上传清洗全流程：4 种校验状态、错误日志、确认入库
- 6 个月时序产值数据（S 曲线/同比环比可直接测）
- 6 条核心校验 SQL

### 阶段 1：底层公共基础设施
- ORM 模型全覆盖：18 业务 + 10 字典 + 4 导入 + 3 系统，全部 SQLAlchemy
- 公共工具：paginate（自动软删除过滤）、金额分/元/万元互转
- 统一异常处理：AppException + 5 子类，全局 JSON 错误响应
- JWT 鉴权 + 角色权限中间件（require_role 工厂函数，数字比较）
- Redis 工具类（连接池 + JSON 序列化 + cache_result 装饰器）
- MinIO 文件工具（上传/下载/删除，自动建桶）
- 字典全局缓存（写操作自动失效，POST /refresh 强制刷新）
- loguru 日志（双输出 + 按天轮转）

### 阶段 2：基础管理模块
- 用户 CRUD（分页/搜索/角色筛选/启停，自删除防护）
- 字典 CRUD（10 表统一接口，is_active 过滤，toggle-active）
- 项目管理 CRUD（编码唯一校验 + 状态筛选 + 基础统计）

## 每日总结保存位置
用户要求每日总结写到 `D:/展望数科/每日总结/` 目录（非项目目录）
