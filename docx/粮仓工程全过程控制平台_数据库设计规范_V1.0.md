# 粮仓工程全过程控制平台

# 数据库设计规范（V1.1）

> **配套文档说明**：
> - 本文档是《系统开发设计规范》的配套文档，规定设计原则、命名规范、约束规则和系统基础设施表
> - 具体的业务表建表 SQL 请参考 **《数据库表设计 V2.0》**
> - V1.1 更新：基于 7 个 Excel 源文件的数据字典分析结果，重构了业务表设计
> - 前后端开发时应以本文档为统一约束标准

---

## 一、设计原则

### 1.1 核心设计理念

| # | 原则 | 说明 |
|---|---|---|
| 1 | **数据底座统一** | 所有最小颗粒度数据进 `t_data_rows`，汇总、对比、报告全部基于此表 |
| 2 | **业务表与流水表分离** | 业务表（如 `t_reports`）只存"当前态"，流水表存"全部历史" |
| 3 | **四元组标记** | 每条数据必须带 `source_type` + `is_confirmed` + `source_doc` + `raw_data` |
| 4 | **金额 BIGINT（分）** | 避免 DECIMAL 浮点精度问题，前端展示时除以 100 |
| 5 | **时间 TIMESTAMPTZ** | 一律带时区 |
| 6 | **软删除优先** | 业务数据不真删，使用 `deleted_at` 标记 |
| 7 | **审计必加** | 所有写操作入 `t_audit_logs` |
| 8 | **合同版本强校验** | DB 层部分唯一索引实现 |

### 1.2 字段类型约定

| 数据类型 | 使用场景 | 约束 |
|---|---|---|
| `UUID` | 所有表的主键、外键引用 | 默认 `gen_random_uuid()` |
| `BIGINT` | 金额类字段 | 单位 = 分（前端展示 ÷ 100） |
| `INTEGER` | 序号、计数、期次 | `period_no`、`version`、`chunk_count` 等 |
| `DATE` | 日期字段 | 无时间部分 |
| `TIMESTAMPTZ` | 时间点字段 | 必须带时区 |
| `VARCHAR(N)` | 短文本 | N 根据业务合理设定上限 |
| `TEXT` | 长文本 | 合同条款、报告内容等 |
| `JSONB` | 动态结构数据 | AI 原始数据、报告负载、模板 schema |
| `BOOLEAN` | 状态开关 | `is_confirmed`、`is_active`、`is_deleted` |
| `DECIMAL(5,2)` | 百分比/比例 | 仅用于比例字段（如 `payment_ratio`） |
| `vector(1024)` | 向量嵌入 | pgvector 扩展，BGE-M3 输出 |

---

## 二、命名规范

### 2.1 表命名规范

| 规则 | 示例 |
|---|---|
| 系统核心表以 `t_` 为前缀 | `t_data_rows` |
| 业务用英文小写复数形式 | `t_contracts`，`t_projects` |
| 多词用下划线分隔 | `t_secondary_cost_ledger` |
| 关联/子表用父表名 + 子表名 | `t_contract_versions`，`t_contract_clauses` |
| 日志/审计表独立命名 | `t_audit_logs`，`t_ai_tasks` |

### 2.2 字段命名规范

| 规则 | 示例 |
|---|---|
| 主键统一为 `id` | `id UUID PRIMARY KEY DEFAULT gen_random_uuid()` |
| 外键统一为 `{父表单数}_id` | `project_id`，`contract_id`，`template_id` |
| 时间字段统一后辍 | `created_at`，`updated_at`，`deleted_at`，`confirmed_at` |
| 布尔字段用 `is_` / `has_` 前缀 | `is_confirmed`，`is_active` |
| 金额字段用 `_amount` 后辍 | `total_amount`，`paid_amount`，`declare_amount` |
| 类型/状态字段用 `_type` / `_status` 后辍 | `source_type`，`report_status`，`project_status` |

### 2.3 索引命名规范

| 索引类型 | 命名格式 | 示例 |
|---|---|---|
| 普通索引 | `idx_{表名}_{字段1}_{字段2}` | `idx_data_rows_project_date` |
| 唯一索引 | `idx_{表名}_unique_{字段}` | `idx_contract_version_unique` |
| 部分唯一索引 | `idx_{表名}_{描述}` | `idx_contract_one_active` |
| 外键索引 | `idx_{表名}_fk_{外键字段}` | `idx_contracts_fk_project_id` |
| 向量索引 | `idx_{表名}_embedding` | `idx_chunks_embedding` |

---

## 三、表关系架构

### 3.1 ER 关系总览

```
t_projects（项目主表） ★ 基于数据字典扩增至 20+ 字段
    │
    ├── 1:N → t_contracts（合同主表）
    │          ├── 1:N → t_contract_payment_stages（支付阶段）  ← 替代 t_contract_versions + t_contract_clauses
    │          └── 1:N → t_contract_monthly_detail（月度明细）  ← 横向月份列转纵向
    │
    ├── 1:N → t_progress_payment_review（进度款审核）           ← 4 类单位共用单表
    │
    ├── 1:N → t_weekly_progress_report（周报-形象进度）
    ├── 1:N → t_weekly_progress_metrics（周报-投资指标）
    ├── 1:N → t_monthly_province_progress（省市月进度）
    ├── 1:N → t_monthly_target_plan（月度目标计划）
    ├── 1:N → t_fund_breakdown（资金来源分解）
    │
    ├── 1:N → t_data_rows（数据流水表） ← 系统核心数据底座
    ├── 1:N → t_reports（报告实例表）
    │          └── N:1 → t_report_templates（报告模板表）
    ├── 1:N → t_knowledge_docs（知识库文档表）
    │          └── 1:N → t_knowledge_chunks（文档分块表）
    └── 1:N → t_ai_tasks（AI 任务表）

字典表（10 张，统一结构）:
    dict_project_nature, dict_invest_timing, dict_invest_nature,
    dict_invest_structure, dict_invest_field, dict_project_level,
    dict_location, dict_owner_unit, dict_supplier, dict_review_type

独立表（不直接关联项目）：
    t_users（用户与权限表）
    t_audit_logs（审计日志表）
```

### 3.2 核心依赖关系

```
                          ┌──────────────────┐
                          │   t_data_rows    │ ← 全系统数据底座
                          │  (单一事实来源)  │
                          └────────┬─────────┘
                                   │ 提供数据
                ┌──────────────────┼──────────────────┐
                ▼                  ▼                  ▼
         ┌──────────┐     ┌──────────┐     ┌──────────────┐
         │ t_reports │     │ t_payment│     │ 驾驶舱统计   │
         │ (报告)   │     │ _audits  │     │ (Dashboard)  │
         └──────────┘     │ (审核)   │     │ (统计服务)   │
                          └──────────┘     └──────────────┘
```

### 3.3 数据流关系

```
上传文件（MinIO）
    ↓
t_knowledge_docs（文件记录，parse_status=pending）
    ↓
AI/Tesseract/PDF 解析（异步任务）
    ↓
t_data_rows（结构化数据，is_confirmed=false，raw_data=AI原始快照）
    ↓
用户确认/修改
    ↓
t_data_rows（is_confirmed=true，confirmed_by/confirmed_at）
    ↓
业务表更新（t_contracts / t_payment_audits / t_reports 等）
    ↓
t_audit_logs（全程记录每次变更）
```

---

## 四、表详细设计

> **V1.1 变更**: 业务表（4.2~4.10）的完整字段定义和建表 SQL 已移至 **《数据库表设计 V2.0》**。本文档仅保留系统基础设施表（4.1 数据流水表、4.13~4.15）的完整定义，以及业务表的核心设计要点（4.2~4.12）。

### 4.1 系统核心：数据流水表（t_data_rows）

**角色**：整个系统的事实源（Single Source of Truth）。所有工程资料最终转换为统一结构化数据存储于此表，系统全部功能均依赖此表运行。

**四元组原则**：每条数据必须带：

| 元组 | 字段 | 说明 |
|---|---|---|
| source_type | `source_type` | 施工方上报/过控审核/AI识别/系统计算 |
| is_confirmed | `is_confirmed` | 是否已人工确认 |
| source_doc | `source_doc` | 来源文档名称 |
| raw_data | `raw_data` | AI 识别原始数据快照（JSONB） |

**表结构**：

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK DEFAULT gen_random_uuid() | |
| project_id | UUID | NOT NULL FK→t_projects | 所属项目 |
| data_date | DATE | NOT NULL | 数据发生日期 |
| category | VARCHAR(100) | NOT NULL | 分部工程类别（地基/主体/装修/二类费用） |
| item_name | VARCHAR(200) | NOT NULL | 分项名称（混凝土C30/钢筋HRB400/模板） |
| planned_quantity | BIGINT | DEFAULT 0 | 计划工程量 |
| actual_quantity | BIGINT | DEFAULT 0 | 实际工程量 |
| unit | VARCHAR(20) | | 单位（m³/t/m²/项） |
| unit_price | BIGINT | DEFAULT 0 | 单价（分） |
| amount | BIGINT | DEFAULT 0 | 金额（分） |
| cost_type | VARCHAR(50) | NOT NULL | 费用类型（建安工程费/二类费用/其他） |
| source_doc | VARCHAR(500) | | 来源文档名称 |
| source_type | VARCHAR(30) | NOT NULL | 来源（constructor/reviewer/ai/system） |
| is_confirmed | BOOLEAN | NOT NULL DEFAULT FALSE | 是否已人工确认 |
| raw_data | JSONB | DEFAULT '{}' | AI 识别原始数据快照 |
| confirmed_by | UUID | FK→t_users | 确认人 |
| confirmed_at | TIMESTAMPTZ | | 确认时间 |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT NOW() | |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT NOW() | |
| deleted_at | TIMESTAMPTZ | | 软删除 |

**关键约束**：

```sql
-- 汇总统计只取已确认的数据
-- WHERE is_confirmed = true AND deleted_at IS NULL
```

**关键索引**（见第七章）。

---

### 4.2 项目主表（t_projects）

**角色**：所有业务数据的根节点，整合产投月报/目标计划/省市重点项目的数据源。

**V1.1 变更**: 基于数据字典分析，字段从 14 个扩增至 **30+ 个**，新增项目性质、投资时序/性质/结构/领域、实施主体/时间、业务划分、重要性、行业主管部门、质量安全目标等字段。完整定义见 V2.0 §2.1。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| project_name | VARCHAR(200) | NOT NULL | 项目名称 |
| project_code | VARCHAR(50) | **UNIQUE NOT NULL** | 项目编码（业务唯一标识） |
| project_nature | VARCHAR(50) | NOT NULL | 功能性项目/商业性项目 |
| invest_timing | VARCHAR(20) | NOT NULL | 新投项目/续投项目 |
| invest_nature | VARCHAR(50) | | 固定资产投资/股权投资/其他投资 |
| invest_structure | VARCHAR(50) | | 基础设施类/民生和社会事业类/生态环保类 |
| invest_field | VARCHAR(100) | | 装备制造/食品饮料/材料化工/... |
| implement_body | VARCHAR(200) | | 实施主体 |
| implement_period | VARCHAR(50) | | 实施时间（如"2025.12-2027.11"） |
| business_class | VARCHAR(20) | | 主业/非主业/培育业 |
| region | VARCHAR(20) | DEFAULT '市内' | 市内/市外 |
| importance | VARCHAR(10) | | ★(省重点)/▲(市重点) |
| supervising_dept | VARCHAR(200) | | 行业归口主管部门 |
| owner_unit | VARCHAR(200) | | 业主单位/建设单位 |
| responsible_unit | VARCHAR(200) | | 责任单位 |
| construction_content | TEXT | | 项目内容/建设内容 |
| construction_scale | TEXT | | 建设内容及规模 |
| construction_period | VARCHAR(50) | | 建设周期（如"600天"） |
| planned_total_invest | BIGINT | NOT NULL DEFAULT 0 | 计划总投资（分） |
| planned_invest_2026 | BIGINT | DEFAULT 0 | 2026年计划投资（分） |
| quality_target | VARCHAR(50) | DEFAULT '合格' | 质量目标 |
| safety_target | VARCHAR(100) | | 安全目标 |
| expected_return | TEXT | | 投资预期收益说明 |
| project_status | VARCHAR(20) | DEFAULT 'preparing' | preparing/constructing/completed/suspended |
| created_at | TIMESTAMPTZ | NOT NULL DEFAULT NOW() | |
| updated_at | TIMESTAMPTZ | NOT NULL DEFAULT NOW() | |
| deleted_at | TIMESTAMPTZ | | 软删除 |

---

### 4.3 合同主表（t_contracts）

**角色**：合同业务入口，来源于二类费用台账/合同支付情况汇总表。

**V1.1 变更**: 字段与源数据表头一一对应（序号/编号/供应商/合同内容/签订日期/合同金额）。编号（CX001格式）为业务主键。完整定义见 V2.0 §2.2。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| project_id | UUID | NOT NULL FK→t_projects | |
| seq | INTEGER | NOT NULL | 行序号 |
| contract_no | VARCHAR(20) | **UNIQUE NOT NULL** | 合同编号（CX001~CX018） |
| supplier_name | VARCHAR(200) | NOT NULL | 供应商/承包单位 |
| contract_desc | VARCHAR(500) | NOT NULL | 合同内容/类型描述 |
| sign_date | DATE | | 签订日期 |
| contract_amount | BIGINT | DEFAULT 0 | 合同金额（分）⚠ 源数据多数为空 |
| contract_type | VARCHAR(30) | NOT NULL DEFAULT 'secondary' | main/secondary/supplementary |
| status | VARCHAR(20) | NOT NULL DEFAULT 'active' | active/expired/terminated |
| created_at | TIMESTAMPTZ | NOT NULL | |
| updated_at | TIMESTAMPTZ | NOT NULL | |
| deleted_at | TIMESTAMPTZ | | |

---

### 4.4 合同支付阶段表（t_contract_payment_stages）

**角色**：替代 V1.0 的 `t_contract_versions` + `t_contract_clauses`。不同合同的付款阶段数不同（3~5个），用子表存储更灵活。完整定义见 V2.0 §2.3。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| contract_id | UUID | NOT NULL FK→t_contracts | |
| contract_no | VARCHAR(20) | NOT NULL | 冗余 |
| stage_order | INTEGER | NOT NULL | 阶段序号（1=施工前/预付款, 2=施工中, 3=竣工, 4=结算后, 5=质保尾款） |
| stage_name | VARCHAR(50) | NOT NULL | 施工前阶段/施工中阶段/竣工阶段/尾款阶段 |
| payment_terms | TEXT | | 支付条款原文 |
| cumulative_paid | BIGINT | DEFAULT 0 | 累计已付（分） |
| remaining_unpaid | BIGINT | DEFAULT 0 | 剩余未付（分） |

**设计说明**: 支付阶段示例：
- CX005（中储粮，设计合同）: 3 阶段 — ①预付款20% ②审批后30% ③初设后80% ④尾款
- CX008（德阳建工，EPC施工）: 5 阶段 — ①* ②按月80% ③验收后90% ④结算后97% ⑤质保3%

---

### 4.5 合同月度明细表（t_contract_monthly_detail）

**角色**: 合同台账中横向展开的 24 个月份列（Col P~AO）转为纵向存储。同时承载支付金额和产值金额。完整定义见 V2.0 §2.4。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| contract_id | UUID | NOT NULL FK→t_contracts | |
| contract_no | VARCHAR(20) | NOT NULL | 冗余 |
| detail_month | DATE | NOT NULL | 月份（如 2026-01-01） |
| payment_amount | BIGINT | DEFAULT 0 | 当月支付金额（分） |
| output_amount | BIGINT | DEFAULT 0 | 当月产值（分） |
| est_cumulative_payable | BIGINT | DEFAULT 0 | 预估累计应支付（分） |
| actual_cumulative_paid | BIGINT | DEFAULT 0 | 实际累计已支付（分） |
| cumulative_completed | BIGINT | DEFAULT 0 | 累计已完成产值（分） |
| remaining_uncompleted | BIGINT | DEFAULT 0 | 剩余未完成产值（分） |

**约束**: `UNIQUE(contract_no, detail_month)`

---

### 4.6 进度款审核表（t_progress_payment_review）

**角色**: 替代 V1.0 的 `t_payment_audits`。4 类单位（设备/设计/施工/监理过控）共用单表，通过 `review_type` 区分。完整定义见 V2.0 §2.5。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| project_id | UUID | NOT NULL FK→t_projects | |
| contract_id | UUID | FK→t_contracts | |
| review_type | VARCHAR(20) | NOT NULL | 设备/设计/施工/监理过控 |
| period_month | VARCHAR(50) | NOT NULL | 期数/月份 |
| period_no | INTEGER | NOT NULL DEFAULT 1 | |
| applicant_unit | VARCHAR(200) | NOT NULL | 申请单位 |
| contract_amount | BIGINT | NOT NULL | 合同金额（分） |
| cumulative_reported_output | BIGINT | NOT NULL | 累计报送产值（分） |
| payment_ratio | DECIMAL(6,4) | | 付款比例 |
| current_audited_output | BIGINT | NOT NULL | 本期审核产值（分） |
| cumulative_audited_payable | BIGINT | NOT NULL | 累计审核应付款（分） |
| audit_status | VARCHAR(20) | DEFAULT 'pending' | pending/auditing/audited/paid |

**扩展字段**: 施工类（`constr_install_fee`, `safety_civil_fee` 等 4 列）/ 监理过控类（`supervision_fee`, `cost_consult_fee`, `settlement_fee` 等 6 列）见 V2.0。

---

### 4.7 周报表（t_weekly_progress_report + t_weekly_progress_metrics）

**V1.1 新增**。两张表分别存储：
- `t_weekly_progress_report`: 各工程部位的周形象进度（来源：周报描述7-1.xlsx）
- `t_weekly_progress_metrics`: 投资完成百分比指标（来源：周报百分比计算表格.xlsx，2列 K-V 布局）

完整定义见 V2.0 §2.6~2.7。

---

### 4.8 月报表（t_monthly_province_progress + t_monthly_target_plan + t_fund_breakdown）

**V1.1 新增**。三张表分别存储：
- `t_monthly_province_progress`: 省市重点项目月度进度（来源：2026年省市重点项目6月进度.xlsx，49 个项目）
- `t_monthly_target_plan`: 川西项目月度目标计划（来源：目标计划表，横向 12 个月转纵向）
- `t_fund_breakdown`: 资金来源分解（来源：续投项目 Sheet，总投资+年度计划两组六类资金）

完整定义见 V2.0 §2.8~2.10。

---

### 4.9 字典表（10 张）

**V1.1 新增**。所有字典表使用统一结构 `(id UUID PK, code VARCHAR(50) UNIQUE, name VARCHAR(100), sort_order INTEGER, is_active BOOLEAN, created_at TIMESTAMPTZ)`。

覆盖：项目性质、投资时序、投资性质、投资结构、投资领域、项目级别、工程部位、业主单位、供应商、审核类型。

完整定义见 V2.0 §3。

---

### 4.10 报告模板表（t_report_templates）

**角色**：模板配置驱动，JSON Schema 定义报告字段，业务变动仅调配置不动代码。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| template_name | VARCHAR(200) | NOT NULL | 模板名称 |
| report_type | VARCHAR(20) | NOT NULL | weekly（周报）/monthly（月报）/payment_report（付款报告） |
| version | INTEGER | NOT NULL DEFAULT 1 | 模板版本号 |
| schema_json | JSONB | **NOT NULL** | JSON Schema 定义（字段、校验规则、布局） |
| word_template_path | VARCHAR(500) | 允许 NULL | Word 模板文件 MinIO 路径 |
| ppt_template_path | VARCHAR(500) | 允许 NULL | PPT 模板 MinIO 路径 |
| is_active | BOOLEAN | NOT NULL DEFAULT FALSE | 是否启用 |
| created_at | TIMESTAMPTZ | NOT NULL | |
| updated_at | TIMESTAMPTZ | NOT NULL | |

**JSON Schema 字段类型限定**（5 种）：

| type | 说明 | 示例 |
|---|---|---|
| `text` | 文本字段 | 进度描述、下周计划 |
| `number` | 数值字段 | 完成产值、工程量 |
| `date` | 日期字段 | 报告周期 |
| `formula` | 公式字段（后端计算） | 累计值 = 上期累计 + 本期完成 |
| `reference` | 引用字段（关联其他表） | 关联合同 ID、项目 ID |

**schema_json 示例**：

```json
{
  "fields": [
    {"key": "week_no", "label": "周次", "type": "text", "required": true},
    {"key": "completed_value", "label": "本周完成产值", "type": "number", "unit": "fen"},
    {"key": "cumulative_value", "label": "累计完成产值", "type": "formula", "formula": "prev.cumulative + this.completed"},
    {"key": "progress_desc", "label": "本周进度描述", "type": "text", "ai_suggest": true}
  ]
}
```

---

### 4.11 报告实例表（t_reports）

**角色**：报告的实例记录，保存报告生成时的完整数据快照，保证历史报告不变。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| project_id | UUID | **NOT NULL FK**→t_projects | |
| template_id | UUID | **NOT NULL FK**→t_report_templates | 使用的模板 |
| report_type | VARCHAR(20) | NOT NULL | weekly/monthly |
| period_start | DATE | NOT NULL | 报告周期开始 |
| period_end | DATE | NOT NULL | 报告周期结束 |
| data_payload | JSONB | **NOT NULL DEFAULT '{}'** | 报告全部字段数据（**生成时快照**） |
| report_status | VARCHAR(20) | DEFAULT 'draft' | draft（草稿）/pending（待审核）/confirmed（已确认）/published（已发布） |
| ai_generated_content | JSONB | DEFAULT '{}' | AI 生成文字建议快照 |
| created_by | UUID | FK→t_users | 创建人 |
| reviewed_by | UUID | 允许 NULL FK→t_users | 审核人 |
| review_comment | TEXT | 允许 NULL | 审核意见 |
| created_at | TIMESTAMPTZ | NOT NULL | |
| updated_at | TIMESTAMPTZ | NOT NULL | |

**关键设计**：
- `data_payload` 保存报告生成时的完整字段快照，**数据库后续修改不影响已生成的报告**
- `ai_generated_content` 保存 AI 建议原文，用于追溯和对比

---

### 4.12 知识库文档表（t_knowledge_docs）

**角色**：上传文件的管理记录，支撑 RAG 检索与知识问答。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| project_id | UUID | 允许 NULL FK→t_projects | 关联项目（NULL = 通用文档） |
| doc_category | VARCHAR(50) | NOT NULL | contract（合同）/drawing（图纸）/visa（签证）/meeting（会议纪要）/standard（规范） |
| doc_title | VARCHAR(500) | NOT NULL | 文档标题 |
| file_path | VARCHAR(500) | NOT NULL | MinIO 路径 |
| file_size | BIGINT | 允许 NULL | 文件大小（字节） |
| doc_summary | TEXT | 允许 NULL | AI 生成摘要 |
| chunk_count | INTEGER | DEFAULT 0 | 分块数量 |
| parse_status | VARCHAR(20) | DEFAULT 'pending' | pending（待解析）/parsing（解析中）/ready（就绪）/failed（失败） |
| uploaded_by | UUID | FK→t_users | 上传人 |
| uploaded_at | TIMESTAMPTZ | NOT NULL | |

**文档分块表（t_knowledge_chunks）**：

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| doc_id | UUID | **NOT NULL FK ON DELETE CASCADE**→t_knowledge_docs | |
| chunk_no | INTEGER | NOT NULL | 分块序号 |
| content | TEXT | NOT NULL | 分块文本 |
| embedding | vector(1024) | | BGE-M3 向量嵌入 |
| tsv | tsvector | | PostgreSQL 全文搜索索引 |
| source_span | VARCHAR(100) | 允许 NULL | 来源位置（如 "p12#tbl3:r4-r7"） |

**约束**：`UNIQUE(doc_id, chunk_no)`

---

### 4.13 审计日志表（t_audit_logs）

**角色**：全系统唯一审计入口，所有写操作必须记录，保留 5 年。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| table_name | VARCHAR(50) | NOT NULL | 操作的表名 |
| record_id | UUID | 允许 NULL | 操作记录 ID |
| field_name | VARCHAR(100) | 允许 NULL | 修改的字段名 |
| old_value | TEXT | 允许 NULL | 修改前值 |
| new_value | TEXT | 允许 NULL | 修改后值 |
| operation_type | VARCHAR(20) | NOT NULL | INSERT/UPDATE/DELETE/CONFIRM/EXPORT |
| modified_by | UUID | 允许 NULL | 操作人（system = NULL） |
| modified_at | TIMESTAMPTZ | NOT NULL DEFAULT NOW() | |
| source | VARCHAR(30) | 允许 NULL | manual（人工）/ai_recognition（AI识别）/system（系统自动） |

**必须记录审计的操作**：

| 操作类型 | 说明 | 必须记录的表 |
|---|---|---|
| INSERT | 新增记录 | 全部 13 张表 |
| UPDATE | 字段修改 | 全部业务表（t_data_rows, t_contracts 等） |
| DELETE | 软删除（设置 deleted_at） | 全部业务表 |
| CONFIRM | 人工确认 AI 识别结果 | t_data_rows（is_confirmed 变更） |
| EXPORT | 报告/数据导出 | t_reports |

---

### 4.14 AI 任务表（t_ai_tasks）

**角色**：追踪所有 AI 异步任务（文档解析、报告生成、审核分析）的执行状态。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| task_type | VARCHAR(50) | NOT NULL | data_recognition（数据识别）/weekly_report（周报生成）/payment_audit（付款审核）/qa（问答） |
| status | VARCHAR(20) | DEFAULT 'pending' | pending（等待）/running（运行中）/success（成功）/failed（失败）/cancelled（取消） |
| input_refs | JSONB | DEFAULT '{}' | 输入引用（文件 ID、表 ID） |
| output | JSONB | 允许 NULL | 任务输出结果 |
| error_message | TEXT | 允许 NULL | 错误消息 |
| started_at | TIMESTAMPTZ | 允许 NULL | 开始时间 |
| finished_at | TIMESTAMPTZ | 允许 NULL | 完成时间 |
| duration_ms | INTEGER | 允许 NULL | 执行耗时（毫秒） |
| operator_id | UUID | 允许 NULL FK→t_users | 操作人 |
| project_id | UUID | 允许 NULL FK→t_projects | 所属项目 |
| created_at | TIMESTAMPTZ | NOT NULL | |

**状态流转**：
```
pending → running → success
                 → failed → pending（重试）
                 → cancelled
```

---

### 4.15 用户与权限表（t_users）

**角色**：用户身份认证与权限管理。

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | UUID | PK | |
| username | VARCHAR(50) | **UNIQUE NOT NULL** | 登录名 |
| password_hash | VARCHAR(255) | NOT NULL | bcrypt 哈希 |
| real_name | VARCHAR(100) | NOT NULL | 真实姓名 |
| email | VARCHAR(200) | **UNIQUE** 允许 NULL | 邮箱 |
| role | VARCHAR(30) | NOT NULL | admin（管理员）/project_manager（项目负责人）/auditor（造价工程师）/operator（资料员）/viewer（只读查看） |
| project_permissions | JSONB | DEFAULT '[]' | 数据权限（项目 ID 数组） |
| is_active | BOOLEAN | DEFAULT TRUE | 是否启用 |
| last_login_at | TIMESTAMPTZ | 允许 NULL | 最后登录时间 |
| created_at | TIMESTAMPTZ | NOT NULL | |
| updated_at | TIMESTAMPTZ | NOT NULL | |
| deleted_at | TIMESTAMPTZ | | 软删除 |

**支持 5 种角色**：

| 角色 | 标识 | 说明 |
|---|---|---|
| 管理员 | admin | 系统配置、用户管理、权限管理 |
| 项目负责人 | project_manager | 查看全部、审批报告、确认数据 |
| 造价工程师 | auditor | 审核进度款、对比分析 |
| 资料员 | operator | 上传资料、编写报告、数据录入 |
| 查看者 | viewer | 只读查看驾驶舱和报告 |

---

## 五、约束规则总表

### 5.1 NULL 约束规则

| 规则 | 适用场景 | 违反后果 |
|---|---|---|
| **允许字段为空** | 非核心业务字段（施工单位、监理单位、付款金额等） | 不阻塞业务对象创建 |
| **NOT NULL 强制** | 主键、外键、创建时间、业务必须字段（项目编号、合同编号等） | API 返回 422 校验失败 |

**必须 NOT NULL 的字段清单**：

| 表 | 必填字段 |
|---|---|
| t_projects | id, project_name, project_code, project_nature, invest_timing, planned_total_invest, project_status, created_at, updated_at |
| t_contracts | id, project_id, seq, contract_no, supplier_name, contract_desc, contract_type, status, created_at, updated_at |
| t_contract_payment_stages | id, contract_id, contract_no, stage_order, stage_name, created_at |
| t_contract_monthly_detail | id, contract_id, contract_no, detail_month, created_at |
| t_progress_payment_review | id, project_id, review_type, period_month, applicant_unit, contract_amount, cumulative_reported_output, current_audited_output, cumulative_audited_payable, audit_status, created_at |
| t_weekly_progress_report | id, project_id, report_date, seq, location_name, created_at |
| t_weekly_progress_metrics | id, project_id, report_week, metric_name, metric_value, created_at |
| t_monthly_province_progress | id, project_id, report_month, seq, project_name, planned_total_invest, created_at |
| t_monthly_target_plan | id, project_id, plan_month, created_at |
| t_fund_breakdown | id, project_id, fund_year, fund_scope, created_at |
| t_data_rows | id, project_id, data_date, category, item_name, cost_type, source_type, is_confirmed, created_at, updated_at |
| t_report_templates | id, template_name, report_type, schema_json, created_at |
| t_reports | id, project_id, template_id, report_type, period_start, period_end, data_payload, report_status, created_at |
| t_knowledge_docs | id, doc_category, doc_title, file_path, parse_status, uploaded_at |
| t_knowledge_chunks | id, doc_id, chunk_no, content |
| t_audit_logs | id, table_name, operation_type, modified_at |
| t_ai_tasks | id, task_type, status, created_at |
| t_users | id, username, password_hash, real_name, role, is_active, created_at |

### 5.2 UNIQUE 约束规则

| 表 | 约束字段 | 说明 |
|---|---|---|
| t_projects | project_code | 项目编码全局唯一 |
| t_contracts | contract_no | 合同编号（CX001格式）全局唯一 |
| t_contract_monthly_detail | (contract_no, detail_month) | 同一合同同一月份唯一 |
| t_weekly_progress_metrics | (project_id, report_week, data_scope, metric_name) | 同一周报同一指标唯一 |
| t_monthly_target_plan | (project_id, plan_month) | 同一项目同一月份唯一 |
| t_fund_breakdown | (project_id, fund_year, fund_scope) | 同一项目同一年度同一范围唯一 |
| t_knowledge_chunks | (doc_id, chunk_no) | 同一文档下分块序号唯一 |
| t_users | username | 用户名唯一 |
| t_users | email | 邮箱唯一 |

### 5.3 部分唯一索引约束

```sql
-- ★★★★★ 核心约束：同一合同同一时间只有一个生效版本
CREATE UNIQUE INDEX idx_contract_one_active
ON t_contract_versions(contract_id) WHERE valid_to IS NULL;
```

### 5.4 CHECK 约束建议

```sql
-- 项目状态枚举
CHECK (project_status IN ('preparing', 'constructing', 'completed', 'suspended'))

-- 合同类型枚举
CHECK (contract_type IN ('main', 'secondary', 'supplementary'))

-- 合同状态枚举
CHECK (status IN ('active', 'expired', 'terminated'))

-- 数据来源枚举
CHECK (source_type IN ('constructor', 'reviewer', 'ai', 'system'))

-- 报告状态枚举
CHECK (report_status IN ('draft', 'pending', 'confirmed', 'published'))

-- 审计状态枚举
CHECK (audit_status IN ('pending', 'auditing', 'audited', 'paid'))

-- 审核类型枚举
CHECK (review_type IN ('设备', '设计', '施工', '监理过控'))

-- 资金范围枚举
CHECK (fund_scope IN ('total', 'annual_plan'))

-- 解析状态枚举
CHECK (parse_status IN ('pending', 'parsing', 'ready', 'failed'))

-- AI 任务状态枚举
CHECK (status IN ('pending', 'running', 'success', 'failed', 'cancelled'))

-- 用户角色枚举
CHECK (role IN ('admin', 'project_manager', 'auditor', 'operator', 'viewer'))

-- 金额不能为负
CHECK (contract_amount >= 0)
CHECK (planned_total_invest >= 0)
CHECK (monthly_planned_invest >= 0)
```

> **注意**：开发时推荐在应用层（Pydantic/SQLAlchemy）做枚举校验，DB 层 CHECK 约束作为加固防线。

---

## 六、金额与时间处理规范

### 6.1 金额统一用"分"（BIGINT）

| 规则 | 说明 |
|---|---|
| 数据库存储 | `BIGINT`，单位 = 分 |
| 前端展示 | 除以 100，格式化千分位，保留 2 位小数 |
| 用户输入 | 按"元"输入，前端自动 `×100` 后传给后端 |
| API 传输 | `BIGINT`（分），接口文档标注单位 |

**涉及金额的字段清单**：

| 表 | 金额字段 |
|---|---|
| t_projects | planned_total_invest, planned_invest_2026 |
| t_data_rows | unit_price, amount |
| t_contracts | contract_amount |
| t_contract_payment_stages | cumulative_paid, remaining_unpaid |
| t_contract_monthly_detail | payment_amount, output_amount, est_cumulative_payable, actual_cumulative_paid, cumulative_completed, remaining_uncompleted |
| t_progress_payment_review | contract_amount, cumulative_reported_output, current_audited_output, cumulative_audited_payable, constr_install_fee, safety_civil_fee, supervision_fee, cost_consult_fee, settlement_fee |
| t_weekly_progress_report | weekly_output_reported, weekly_output_audited, cumulative_output_reported, cumulative_output_audited, type2_expense, total_amount |
| t_monthly_province_progress | planned_total_invest, cumulative_invest_by_2025, planned_invest_2026, completed_invest_1_5m, completed_invest_6m, completed_invest_1_6m |
| t_monthly_target_plan | monthly_planned_invest, monthly_actual_invest |
| t_fund_breakdown | central_province_fund, special_general_bond, national_bond_fund, self_owned_fund, financing_fund, other_fund, cumulative_completed_invest, annual_planned_invest |

**前端的金额格式化工具函数**：

```typescript
// 分 → 元，展示
function formatFenToYuan(fen: number): string {
  return (fen / 100).toLocaleString('zh-CN', { minimumFractionDigits: 2 });
}

// 元 → 分，传给后端
function yuanToFen(yuan: number): number {
  return Math.round(yuan * 100);
}
```

### 6.2 时间统一 TIMESTAMPTZ

| 规则 | 说明 |
|---|---|
| 时区 | 全部 `TIMESTAMPTZ` 存储，数据库时区 UTC |
| API 传输 | ISO 8601 格式（如 `2026-07-01T14:30:00+08:00`） |
| 前端展示 | 转换为用户本地时区 |
| 纯日期 | 使用 `DATE` 类型（如 `data_date` `period_start` `valid_from`） |
| 自动维护 | `created_at` 默认 `NOW()`，`updated_at` 由触发器自动更新 |

**updated_at 触发器**：

```sql
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 对每个业务表执行
CREATE TRIGGER trg_{表名}_updated_at
BEFORE UPDATE ON {表名}
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

---

## 七、索引策略

### 7.1 核心索引清单

| 表 | 索引 | 类型 | 说明 |
|---|---|---|---|
| t_data_rows | `(project_id, data_date DESC)` | BTREE | 按项目 + 日期查询流水数据 |
| t_data_rows | `(is_confirmed)` WHERE `is_confirmed=true` | 部分索引 | 加速已确认数据统计 |
| t_data_rows | `(project_id, category, data_date)` | BTREE | 按分部工程查询 |
| t_contracts | `(project_id)` | BTREE | 按项目查询合同 |
| t_contracts | `(contract_no)` | BTREE | 按编号查询 |
| t_contract_payment_stages | `(contract_id)` | BTREE | 按合同查支付阶段 |
| t_contract_monthly_detail | `(contract_id)` | BTREE | 按合同查月度明细 |
| t_contract_monthly_detail | `(detail_month)` | BTREE | 按月份汇总 |
| t_progress_payment_review | `(project_id, review_type)` | BTREE | 按项目+审核类型 |
| t_progress_payment_review | `(audit_status)` | BTREE | 按审核状态筛选 |
| t_weekly_progress_report | `(project_id, report_date)` | BTREE | 按项目+周报日期 |
| t_weekly_progress_metrics | `(project_id, report_week)` | BTREE | 按项目+周次 |
| t_monthly_province_progress | `(project_id, report_month)` | BTREE | 按项目+月份 |
| t_monthly_target_plan | `(project_id, plan_month)` | BTREE | 按项目+月份 |
| t_fund_breakdown | `(project_id, fund_year)` | BTREE | 按项目+年度 |
| t_reports | `(project_id, report_type, period_start)` | BTREE | 按项目+类型+周期查报告 |
| t_knowledge_docs | `(project_id, doc_category)` | BTREE | 按项目+分类查文档 |
| t_knowledge_chunks | `(doc_id, chunk_no)` | UNIQUE BTREE | 文档分块唯一 |
| t_knowledge_chunks | `(embedding)` | ivfflat (lists=100) | 向量近似搜索 |
| t_knowledge_chunks | `(tsv)` | GIN | FTS 全文搜索 |
| t_audit_logs | `(table_name, record_id)` | BTREE | 按表+记录查审计 |
| t_audit_logs | `(modified_at DESC)` | BTREE | 按时间查审计 |
| t_ai_tasks | `(project_id, task_type, status)` | BTREE | 按项目+类型查任务 |
| t_users | `(username)` | UNIQUE BTREE | 登录名唯一 |

### 7.2 索引设计原则

1. **高频查询优先**：针对 `t_data_rows` 建立全覆盖索引
2. **组合索引按最左前缀**：`(project_id, data_date DESC)` 支持 `WHERE project_id=?`
3. **部分索引缩小体积**：`WHERE is_confirmed=true` 的索引只索引已确认数据
4. **向量索引选择**：起步阶段用 `ivfflat`（lists=100），数据量大后切换 `hnsw`
5. **FTS 全文搜索**：`tsv` 字段由触发器自动维护

---

## 八、数据生命周期与状态机

### 8.1 文件解析状态

```
                ┌──────────┐
                │  pending  │ 上传完成，等待解析
                └─────┬────┘
                      │
                      ▼
                ┌──────────┐
                │  parsing  │ AI 正在解析
                └─────┬────┘
                      │
             ┌────────┴────────┐
             ▼                 ▼
      ┌──────────┐      ┌──────────┐
      │   ready   │      │  failed   │
      │ 就绪可用  │      │ 解析失败  │
      └──────────┘      └──────────┘
             │                 │
             │           可重新上传
             │            或人工录入
```

### 8.2 数据确认状态

```
              ┌──────────────┐
              │  AI 识别结果  │
              │ is_confirmed │
              │   = false    │
              └──────┬───────┘
                     │
            ┌────────┴────────┐
            ▼                 ▼
    ┌──────────────┐  ┌──────────────┐
    │ 人工确认通过  │  │ 人工修改后    │
    │ is_confirmed │  │ 确认通过      │
    │   = true     │  │ is_confirmed  │
    │              │  │   = true      │
    └──────────────┘  └──────────────┘
            │                 │
            └────────┬────────┘
                     ▼
            ┌──────────────┐
            │ 参与汇总统计  │
            │ 进入报告/驾驶 │
            │ 舱展示        │
            └──────────────┘
```

### 8.3 报告状态流转

```
draft（草稿） → pending（待审核） → confirmed（已确认） → published（已发布）
      ↑                            ↓
      └──────────────────── 驳回修改
```

### 8.4 审核状态流转

```
pending（待审） → auditing（审核中） → audited（已审） → paid（已付）
      ↑               ↓
      └─────── 退回修改
```

---

## 九、SQL 迁移规范

### 9.1 迁移管理

- 使用 Alembic 管理数据库迁移
- 每个迁移文件必须包含 `upgrade()` 和 `downgrade()`
- 迁移命名规范：`{YYYYMMDD_HHMM}_{描述}.py`
- **严禁手动修改已应用的迁移**（只提交新迁移）

### 9.2 迁移模板

```python
"""create t_data_rows table

Revision ID: abc123
Revises: prev_revision
Create Date: 2026-07-02 11:00:00
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID, JSONB

revision = 'abc123'
down_revision = 'prev_revision'

def upgrade():
    op.create_table('t_data_rows',
        sa.Column('id', UUID, primary_key=True, server_default=sa.text('gen_random_uuid()')),
        sa.Column('project_id', UUID, sa.ForeignKey('t_projects.id'), nullable=False),
        sa.Column('data_date', sa.Date, nullable=False),
        # ...
        sa.Column('created_at', sa.DateTime(timezone=True), server_default=sa.text('NOW()'), nullable=False),
    )
    op.create_index('idx_data_rows_project_date', 't_data_rows', ['project_id', sa.text('data_date DESC')])

def downgrade():
    op.drop_index('idx_data_rows_project_date')
    op.drop_table('t_data_rows')
```

### 9.3 扩展安装（首次迁移）

```sql
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";
CREATE EXTENSION IF NOT EXISTS "vector";
```

---

## 十、扩展与新表规范

### 10.1 新增表检查清单

当后续需要新增业务模块时，按以下清单自检：

- [ ] 表名是否符合 `t_` 前缀 + 英文小写复数命名规范？
- [ ] 是否包含 `id`（UUID PK）？
- [ ] 是否包含 `created_at` 和 `updated_at`（TIMESTAMPTZ）？
- [ ] 需要软删除时，是否包含 `deleted_at`（TIMESTAMPTZ）？
- [ ] 金额字段是否使用 `BIGINT`（分）？
- [ ] 外键是否指向正确的父表？
- [ ] 是否需要唯一约束？
- [ ] 是否在 `t_audit_logs` 中记录了写操作？
- [ ] 字段是否遵循 NULL 约束规则？
- [ ] 是否需要添加索引？
- [ ] 是否需要与 `t_data_rows` 建立数据同步关系？

### 10.2 禁止的操作

| 禁止操作 | 理由 |
|---|---|
| 直接删除业务数据（使用软删除） | 数据不可恢复 |
| DECIMAL 存储金额 | 存在浮点精度问题 |
| 没有 created_at 的表 | 无法追溯创建时间 |
| 没有 updated_at 触发器的表 | 更新时间无法自动维护 |
| 跳过审计日志的写操作 | 操作不可追溯 |
| 直接覆盖旧数据 | 违反版本管理和历史追溯原则 |

---

## 十一、附录：常用查询模式

### 11.1 按项目 + 时间范围查询数据

```sql
SELECT * FROM t_data_rows
WHERE project_id = :project_id
  AND data_date BETWEEN :start_date AND :end_date
  AND is_confirmed = true
  AND deleted_at IS NULL
ORDER BY data_date DESC, category, item_name;
```

### 11.2 汇总统计（已确认数据）

```sql
SELECT category,
       SUM(amount) as total_amount,
       SUM(actual_quantity) as total_quantity
FROM t_data_rows
WHERE project_id = :project_id
  AND data_date <= :end_date
  AND is_confirmed = true
  AND deleted_at IS NULL
GROUP BY category
ORDER BY category;
```

### 11.3 资料完整性统计

```sql
SELECT doc_category,
       COUNT(*) as total,
       SUM(CASE WHEN parse_status = 'ready' THEN 1 ELSE 0 END) as ready_count
FROM t_knowledge_docs
WHERE project_id = :project_id
  AND deleted_at IS NULL
GROUP BY doc_category;
```

### 11.4 审计历史查询

```sql
SELECT * FROM t_audit_logs
WHERE table_name = 't_data_rows'
  AND record_id = :record_id
ORDER BY modified_at DESC;
```

### 11.5 合同支付阶段查询

```sql
SELECT cps.*
FROM t_contract_payment_stages cps
WHERE cps.contract_no = :contract_no
  AND cps.stage_order >= 1
ORDER BY cps.stage_order;
```

### 11.6 月度支付汇总

```sql
SELECT detail_month,
       SUM(payment_amount) as total_payment,
       SUM(output_amount) as total_output
FROM t_contract_monthly_detail
WHERE project_id = :project_id
GROUP BY detail_month
ORDER BY detail_month;
```

### 11.7 待确认数据清单

```sql
SELECT * FROM t_data_rows
WHERE project_id = :project_id
  AND is_confirmed = false
  AND deleted_at IS NULL
ORDER BY created_at DESC;
```

### 11.8 AI 任务状态统计

```sql
SELECT status, COUNT(*) as count
FROM t_ai_tasks
WHERE project_id = :project_id
GROUP BY status;
```

---

## 附录 A：表名与中文名速查

| 序号 | 表名 | 中文名 | 分类 |
|---|---|---|---|
| 1 | `t_projects` | 项目主表 | 业务表 |
| 2 | `t_contracts` | 合同主表 | 业务表 |
| 3 | `t_contract_payment_stages` | 合同支付阶段表 | 子表 |
| 4 | `t_contract_monthly_detail` | 合同月度明细表 | 子表 |
| 5 | `t_progress_payment_review` | 进度款审核表 | 业务表 |
| 6 | `t_weekly_progress_report` | 周报-形象进度 | 业务表 |
| 7 | `t_weekly_progress_metrics` | 周报-投资指标 | 业务表 |
| 8 | `t_monthly_province_progress` | 省市重点项目月进度 | 业务表 |
| 9 | `t_monthly_target_plan` | 月度目标计划 | 业务表 |
| 10 | `t_fund_breakdown` | 资金来源分解 | 业务表 |
| 11 | `t_data_rows` | 数据流水表 | **系统核心** |
| 12 | `t_report_templates` | 报告模板表 | 配置表 |
| 13 | `t_reports` | 报告实例表 | 业务表 |
| 14 | `t_knowledge_docs` | 知识库文档表 | 基础表 |
| 15 | `t_knowledge_chunks` | 文档分块表 | 子表 |
| 16 | `t_ai_tasks` | AI 任务表 | 系统表 |
| 17 | `t_audit_logs` | 审计日志表 | 日志表 |
| 18 | `t_users` | 用户与权限表 | 系统表 |
| D01~D10 | `dict_*` | 字典表（10 张） | 字典表 |

> 注：V1.1 新增 5 张业务表（t_contract_payment_stages, t_contract_monthly_detail, t_weekly_progress_report, t_weekly_progress_metrics, t_monthly_province_progress, t_monthly_target_plan, t_fund_breakdown, t_progress_payment_review）和 10 张字典表。废弃 `t_payment_audits` 和 `t_secondary_cost_ledger`（功能合并到新表）。

> 完整建表 SQL 和字段详情请参阅《数据库表设计 V2.0》。

---

## 附录 B：扩展清单

| 扩展名 | 用途 | 安装方式 |
|---|---|---|
| `uuid-ossp` | UUID 生成（`gen_random_uuid()`） | `CREATE EXTENSION IF NOT EXISTS "uuid-ossp"` |
| `pgcrypto` | 密码加密扩展 | `CREATE EXTENSION IF NOT EXISTS "pgcrypto"` |
| `pg_trgm` | 模糊搜索（`similarity()`） | `CREATE EXTENSION IF NOT EXISTS "pg_trgm"` |
| `vector` | 向量嵌入存储（pgvector） | `CREATE EXTENSION IF NOT EXISTS "vector"` |

---

*本文档为《粮仓工程全过程控制平台》数据库设计规范 V1.1（基于数据字典分析修正），应与《数据库表设计 V2.0》及《系统开发设计规范》配套使用。*
*编制日期：2026 年 7 月*
